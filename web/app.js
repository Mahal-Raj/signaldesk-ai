const $=id=>document.getElementById(id);document.querySelectorAll("[data-sample]").forEach(button=>button.onclick=()=>{$("incident").value=button.dataset.sample});
function render(data){$("result").className="result panel";$("result").innerHTML=`<div class="panel-title"><span>02</span><h2>Inference report</h2><small>Explainable output</small></div><div class="result-grid"><div class="signal"><span>CLASSIFICATION</span><strong class="accent">${data.category}</strong></div><div class="signal"><span>CONFIDENCE</span><strong>${Math.round(data.confidence*100)}%</strong></div><div class="signal"><span>PRIORITY</span><strong>${data.severity}</strong></div></div><div class="block"><span class="label">ROUTE TO</span><div class="runbook"><b>${data.team}</b><br><small>${data.runbook}</small></div></div><div class="block"><h3>MODEL EVIDENCE</h3><div class="chips">${data.evidence.map(x=>`<i>${x}</i>`).join("")||"<small>No strong lexical feature</small>"}</div></div><div class="block"><h3>NEAREST RESOLVED CASES</h3>${data.similar.map(x=>`<div class="match"><b>${x.id} · ${x.category}</b><span>${Math.round(x.similarity*100)}% match</span></div>`).join("")}</div><div class="block"><h3>SANITIZED INPUT</h3><div class="redacted">${data.redacted_text.replaceAll("&","&amp;").replaceAll("<","&lt;")}</div></div>`}
$("analyze").onclick=async()=>{try{$("message").textContent="Redacting sensitive data and running local inference…";const response=await fetch("/api/analyze",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text:$("incident").value})});const data=await response.json();if(!response.ok)throw Error(data.error);render(data);$("message").textContent="Analysis complete · no incident text left this process"}catch(error){$("message").textContent=error.message}};

